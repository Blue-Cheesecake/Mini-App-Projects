import 'package:flutter/material.dart';

class C {
  static Color darkCyan = const HSLColor.fromAHSL(1, 158, 0.36, 0.37).toColor();
  static Color cream = const HSLColor.fromAHSL(1, 30, 0.38, 0.92).toColor();
  static Color veryDarkBlue =
      const HSLColor.fromAHSL(1, 212, 0.21, 0.14).toColor();
  static Color darkGrayishBlue =
      const HSLColor.fromAHSL(1, 228, 0.12, 0.48).toColor();
}
