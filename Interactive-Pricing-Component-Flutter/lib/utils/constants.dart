import 'package:flutter/material.dart';

class ComponentColor {
 static final sliderBackground = const HSLColor.fromAHSL(1, 174, 0.86, 0.45).toColor();
 static final toggleBackground = const HSLColor.fromAHSL(1, 223, 0.5, 0.87).toColor();
 static final discountBackground = const HSLColor.fromAHSL(1, 14, 0.92, 0.95).toColor();
 static final mainBackground = const HSLColor.fromAHSL(1, 230, 1, 0.99).toColor();
 static final pricingCompBackground = const HSLColor.fromAHSL(1, 0, 0, 1).toColor();
 static final textCTABackground = const HSLColor.fromAHSL(1, 227, 0.35, 0.25).toColor();
 static final fullSliderBar = const HSLColor.fromAHSL(1, 174, 0.77, 0.80).toColor();
 static final emptySliderBar = const HSLColor.fromAHSL(1, 224, 0.65, 0.95).toColor();
 static final discountText = const HSLColor.fromAHSL(1, 15, 1, 0.70).toColor();
 static final ctaText = const HSLColor.fromAHSL(1, 226, 1, 0.87).toColor();
 static final text = const HSLColor.fromAHSL(1, 225, 0.20, 0.60).toColor();
}

class HomeTitle {
 static const title = "Simple, traffic-based pricing";
 static const subTitle = "Sign-up for our 30-day trial.\n No credit card required.";
}

class HomeCard {
 static const title = "100K PAGEVIEWS";
 static const perMonthText = "/ month";
 static const monthlyBillText = "Monthly Billing";
 static const yearlyBillText = "Yearly Billing";
}