import 'package:flutter/material.dart';

/// The Menu Bar Displays only Web
///
class MenuBar extends StatelessWidget {
  const MenuBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
