import 'package:flutter/material.dart';

class CalculateButtonWD extends StatelessWidget {
  const CalculateButtonWD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
