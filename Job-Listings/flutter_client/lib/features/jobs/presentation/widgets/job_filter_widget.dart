import 'package:flutter/material.dart';

class JobFilterWidget extends StatelessWidget {
  const JobFilterWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 100,
      child: const Placeholder(),
    );
  }
}
