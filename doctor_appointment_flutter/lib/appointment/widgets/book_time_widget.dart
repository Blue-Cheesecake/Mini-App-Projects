import 'package:flutter/material.dart';

class BookTimeWidget extends StatelessWidget {
  const BookTimeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
