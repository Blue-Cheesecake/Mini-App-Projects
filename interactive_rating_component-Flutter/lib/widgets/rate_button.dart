import 'package:flutter/material.dart';

class RateButton extends StatelessWidget {
  const RateButton({Key? key, required this.score, required this.isSelected})
      : super(key: key);

  final int score;
  final bool isSelected;


  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
