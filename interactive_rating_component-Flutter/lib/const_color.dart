import 'package:flutter/material.dart';

class ConstColor {
  static final orange = const HSLColor.fromAHSL(1, 25, 0.97, 0.53).toColor();
  static final lightGrey =
      const HSLColor.fromAHSL(1, 217, 0.12, 0.63).toColor();
  static final mediumGrey =
      const HSLColor.fromAHSL(1, 216, 0.12, 0.54).toColor();
  static final darkBlue = const HSLColor.fromAHSL(1, 213, 0.19, 0.18).toColor();
  static final veryDarkBlue =
      const HSLColor.fromAHSL(1, 216, 0.12, 0.08).toColor();
  static final containerBg =
      const HSLColor.fromAHSL(1, 215, 0.23, 0.14).toColor();
}
