class Product {
  Product(this.imagePath, this.productType, this.title, this.description,
      this.originalPrice, this.realPrice);

  String imagePath;
  String productType;
  String title;
  String description;
  double originalPrice;
  double realPrice;
}
